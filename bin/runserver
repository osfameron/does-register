#!/bin/bash

sass --update public/stylesheets

echo Note this server will NOT restart 
echo \(looks like plackup doesn\'t work with PocketIO and -r\)

eval `bin/setenv`

plackup -Ilib -a does-register.psgi
