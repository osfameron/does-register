#!/usr/bin/env perl

use strict; use warnings;

use Dir::Self;
BEGIN { chdir __DIR__ . '/..' }
use lib 'lib';
use lib 't/lib';

use DoES::Register::Management;
DoES::Register::Management->new_with_cmd;
